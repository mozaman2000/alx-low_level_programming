#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int nums = 0;

	while (nums < 55)
	{
		if (nums + 48 == 58)
		{
			nums == 49;
		}
		putchar(48 + nums);
		
		++nums;
	}
	putchar('\n');
	return (0);
}
